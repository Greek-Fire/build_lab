---
- name: Ensure system is RHEL 9
  ansible.builtin.assert:
    that:
      - ansible_distribution == "RedHat"
      - ansible_distribution_major_version == "9"
    fail_msg: "This role only supports RHEL 9."

# -----------------------------
# Subscription / CDN
# -----------------------------

- name: Check subscription status
  ansible.builtin.command: subscription-manager status
  register: rhsm_status
  changed_when: false
  failed_when: false

- name: Register using activation key
  redhat_subscription:
    state: present
    org_id: "{{ rhsm_org_id }}"
    activationkey: "{{ rhsm_activationkey }}"
    force_register: "{{ rhsm_force_register }}"
  when:
    - rhsm_status.rc != 0
    - rhsm_org_id is defined
    - rhsm_activationkey is defined

- name: Register using username/password
  redhat_subscription:
    state: present
    username: "{{ rhsm_username }}"
    password: "{{ rhsm_password }}"
    auto_attach: "{{ rhsm_auto_attach | default(true) }}"
    force_register: "{{ rhsm_force_register | default(true) }}"
  when:
    - rhsm_status.rc != 0
    - rhsm_username is defined
    - rhsm_password is defined
    - rhsm_activationkey is not defined

- name: Force refresh subscriptions
  ansible.builtin.command: subscription-manager refresh
  changed_when: false

- name: Enable required repos
  rhsm_repository:
    name: "{{ rhsm_enable_repos }}"
    state: enabled
  when: rhsm_enable_repos is defined

# -----------------------------
# Kernel Validation
# -----------------------------

- name: Get running kernel
  ansible.builtin.command: uname -r
  register: running_kernel
  changed_when: false

- name: Get default kernel via grubby
  ansible.builtin.command: grubby --default-kernel
  register: default_kernel
  changed_when: false
  failed_when: false

- name: Get installed kernel RPMs
  ansible.builtin.shell: |
    rpm -qa | grep ^kernel
  register: kernel_rpms
  changed_when: false

- name: Debug kernel info
  ansible.builtin.debug:
    msg:
      - "Running kernel: {{ running_kernel.stdout }}"
      - "Default kernel: {{ default_kernel.stdout | default('N/A') }}"
      - "Installed kernels: {{ kernel_rpms.stdout_lines }}"

- name: Fail if kernel is wrong major version
  ansible.builtin.assert:
    that:
      - running_kernel.stdout is match('^' ~ expected_kernel_major)
    fail_msg: "Kernel version is not expected ({{ running_kernel.stdout }})"

# -----------------------------
# Install Virtualization Packages
# -----------------------------

- name: Install virtualization stack
  ansible.builtin.dnf:
    name: "{{ virtualization_packages }}"
    state: present

- name: Validate virtualization support
  ansible.builtin.command: virt-host-validate
  register: virt_validate
  changed_when: false
  failed_when: false

- name: Show validation output
  ansible.builtin.debug:
    var: virt_validate.stdout_lines